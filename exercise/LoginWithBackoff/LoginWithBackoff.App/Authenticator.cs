using LoginWithBackoff.App.Models;

namespace LoginWithBackoff.App
{
    public class Authenticator
    {
        public LoginResult Login(string username, string password, DateTime currentTime)
        {
            throw new NotImplementedException();
        }
    }
}
